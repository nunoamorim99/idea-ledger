---
description: Snapshot of the Idea-Ledger — counts by state, in progress, blocked, unreleased, latest version.
argument-hint: (none)
---

Run the `idea-ledger` skill's **status** operation. Read-only — change nothing.

Read `Idea-Ledger/Backlog.md` and `Idea-Ledger/VersionControl.md` (and `Delivered.md` if present)
and report briefly:
- Counts by state: open / in progress / delivered / paused / discarded.
- What's currently **In progress**.
- Anything **blocked** — an open idea whose `[[IDEA-xxx]]` dependency hasn't shipped yet.
- What's sitting in `🚧 Unreleased` awaiting a release, and any `📌 Planned` versions.
- The latest cut version.

If `Area` tags are in use, group open work by area. Keep it short and scannable.
